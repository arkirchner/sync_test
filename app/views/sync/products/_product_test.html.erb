<div id=<%= "product_#{product.id}" %>
  <p><%= product.products %>
  <%= link_to 'Edit', edit_product_path(product), remote: true  %>
  <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' }, remote: true %></p>
</div>
